# 子串（Substring）和子序列（Subsequence）的区别

子串（Substring）和子序列（Subsequence）是字符串处理中的两个相关但不同的概念，它们的主要区别在于字符的连续性：

## 子串（Substring）

- **定义**：子串是原始字符串中连续的一部分。
- **特点**：子串中的字符在原始字符串中是相邻的，不能跳过任何字符。
- **例子**：
  - 原始字符串：`"abcdef"`
  - 可能的子串：`"abc"`, `"bcd"`, `"def"`, `"abcdef"`（本身也是自己的子串）

## 子序列（Subsequence）

- **定义**：子序列是原始字符串中字符的一个序列，但这些字符不一定需要连续。
- **特点**：子序列可以跳过原始字符串中的一些字符，只要保持字符的相对顺序即可。
- **例子**：
  - 原始字符串：`"abcdef"`
  - 可能的子序列：`"ace"`, `"bdf"`, `"abc"`, `"def"`, `"abcdef"`（本身也是自己的子序列）
