# 设计模式

## 创建型模式

- **单例模式（Singleton）**

  - 确保一个类只有一个实例，并提供全局访问点
  - 适用场景：数据库连接、配置管理、日志记录器

- **工厂方法（Factory Method）**

  - 定义一个创建对象的接口，让子类决定实例化哪个类
  - 适用场景：对象的创建逻辑需要集中管理时

- **抽象工厂（Abstract Factory）**

  - 创建一系列相关或相互依赖的对象
  - 适用场景：需要创建一组相关产品时

- **建造者模式（Builder）**
  - 分步骤构建一个复杂对象
  - 适用场景：对象构建过程复杂，需要分步完成

## 结构型模式

- **适配器（Adapter）**

  - 将一个类的接口转换成客户期望的另一个接口
  - 适用场景：集成不兼容的接口

- **装饰器（Decorator）**

  - 动态地给对象添加额外的职责
  - 适用场景：需要动态扩展对象功能时

- **代理（Proxy）**
  - 为其他对象提供一种代理以控制对这个对象的访问
  - 适用场景：远程代理、虚拟代理、保护代理

## 行为型模式

- **观察者（Observer）**

  - 定义对象间的一对多依赖关系
  - 适用场景：事件处理系统、消息通知

- **策略（Strategy）**

  - 定义一系列算法，把它们封装起来，并使它们可以互相替换
  - 适用场景：算法可以互相替换的场景

- **命令（Command）**
  - 将请求封装成对象，使不同的请求可以用相同的方式处理
  - 适用场景：任务队列、命令日志

## 设计模式的原则

1. **单一职责原则**：一个类只负责一个功能领域中的相应职责
2. **开闭原则**：对扩展开放，对修改关闭
3. **里氏替换原则**：所有引用基类的地方必须能透明地使用其子类对象
4. **依赖倒转原则**：依赖于抽象而不是具体实现
5. **接口隔离原则**：使用多个专门的接口，而不使用单一的总接口
