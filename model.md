# 自然语言处理（NLP）

## 基础概念

### 机器学习基础术语

- **模型（Model）**：一个可以从数据中学习并做出预测的算法系统
- **训练（Training）**：使用数据教会模型识别模式的过程
- **推理（Inference）**：模型对新数据进行预测的过程
- **参数（Parameters）**：模型在训练过程中学习到的变量值
- **超参数（Hyperparameters）**：在训练前人为设定的参数，如学习率、批次大小等

### 常见模型类型

- **神经网络（Neural Network）**：
  - 模仿人脑神经元连接的深度学习模型
  - 包含输入层、隐藏层和输出层
- **转换器（Transformer）**：

  - 目前最流行的 NLP 模型架构
  - 使用自注意力机制处理序列数据
  - 代表模型：BERT、GPT 系列

- **循环神经网络（RNN）**：
  - 特别适合处理序列数据的神经网络
  - 包括 LSTM、GRU 等变体

### 重要技术概念

- **词嵌入（Word Embedding）**：

  - 将文字转换为密集向量表示
  - 常见模型：Word2Vec、GloVe

- **注意力机制（Attention Mechanism）**：

  - 让模型能够关注输入中的重要部分
  - 是现代 NLP 模型的核心组件

- **微调（Fine-tuning）**：
  - 在预训练模型基础上进行针对性训练
  - 可以适应特定任务需求

### 评估指标

- **准确率（Accuracy）**：正确预测的比例
- **精确率（Precision）**：正确正例预测占所有正例预测的比例
- **召回率（Recall）**：正确正例预测占所有实际正例的比例
- **F1 分数**：精确率和召回率的调和平均值
